# 签名配置文件模板
# 复制此文件为 keystore.properties 并填写实际的签名信息
# 注意：keystore.properties 文件已添加到 .gitignore，不会被提交到版本控制

# Keystore 文件路径（相对于项目根目录）
storeFile=app/release.keystore

# Keystore 密码
storePassword=your_store_password

# Key 别名
keyAlias=release

# Key 密码
keyPassword=your_key_password

